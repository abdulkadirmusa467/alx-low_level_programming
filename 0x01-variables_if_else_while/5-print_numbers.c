#include <stdio.h>
/**
 * main - Program entry point
 *
 * Return: 0 on success. Error code otherwise
 */
int main(voiid)
{
int n;
for (n = 0; n < 10; n++)
printf("%d", n);
printf("\n");
return (0);
}
